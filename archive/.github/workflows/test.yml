name: Test

on:
  push:
  pull_request:

jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run fresh Docker integration suite
        run: ./tests/test_fresh_bootstrap.sh
